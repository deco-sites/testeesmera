name: Full frontend E2E follow-up

on:
  push:
    branches:
      - agent/full-frontend-e2e-audit-20260812
    paths:
      - "scripts/ci/full-frontend-audit-followup.mjs"
      - ".github/workflows/full-frontend-e2e-followup.yml"

permissions:
  contents: read

jobs:
  follow-up:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      PAYLOAD_API_URL: https://esmeracms-green.vercel.app
      PORT: "8000"
      BASE_URL: http://127.0.0.1:8000
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Build
        run: deno task build
      - name: Start preview
        shell: bash
        run: |
          deno task preview > /tmp/esmera-followup-preview.log 2>&1 &
          for attempt in $(seq 1 120); do
            if curl --silent --fail http://127.0.0.1:8000/ > /dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/esmera-followup-preview.log
          exit 1
      - name: Install Chromium
        run: |
          npm install --no-save playwright@1.54.2
          npx playwright install --with-deps chromium
      - name: Resolve ambiguous findings
        run: node scripts/ci/full-frontend-audit-followup.mjs
      - name: Upload follow-up evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: full-frontend-e2e-followup-pack
          path: full-frontend-followup-artifacts
          if-no-files-found: warn
          retention-days: 14
