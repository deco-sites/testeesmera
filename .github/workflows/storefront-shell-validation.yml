name: Storefront shell validation

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  gate:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    timeout-minutes: ${{ matrix.timeout }}
    env:
      PAYLOAD_API_URL: https://esmeracms-green.vercel.app
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: format
            timeout: 5
            command: deno fmt --check islands/EsmeraHeader.tsx static/esmera-commerce-refine.css tests/shell/storefront_shell_test.tsx
          - name: lint
            timeout: 6
            command: deno lint
          - name: typecheck
            timeout: 8
            command: deno check dev.ts main.ts
          - name: shell-tests
            timeout: 7
            command: deno test -A tests/shell/storefront_shell_test.tsx
          - name: full-tests
            timeout: 10
            command: deno test -A
          - name: build
            timeout: 14
            command: deno task build
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Run ${{ matrix.name }}
        shell: bash
        env:
          GATE_COMMAND: ${{ matrix.command }}
        run: |
          set +e
          bash -lc "$GATE_COMMAND" > /tmp/gate.log 2>&1
          status=$?
          echo "===== ${{ matrix.name }}: last 300 lines ====="
          tail -n 300 /tmp/gate.log
          exit "$status"
